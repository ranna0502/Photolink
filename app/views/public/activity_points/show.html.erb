<div><h1>今日の活動地点</h1></div>
<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <!--infoの部分テンプレート引用-->
      <!--< render 'users/info', user: @user %>-->

      <h6>活動地点へ行ってみましょう！</h6>

        <div>
          活動日時：<%= @activity_point.created_at.strftime('%Y/%m/%d %H:%M') %>
        </div>
        <div>
          人数：<%= @activity_point.person %>
        </div>
        <div>
          スポット：<%= @activity_point.spot %>
        </div>
        <div>
          地点住所：<%= @activity_point.prefecture %><%= @activity_point.address %>
        </div>
        <div>
          登録ステータス：<%= @activity_point.activity_status_i18n %>
        </div>
        <div>
          要望：<%= @activity_point.request %>
        </div>

        <%= link_to '編集する', edit_activity_point_path(@activity_point), class: "btn btn-sm btn-success" %>
        <%= link_to '削除する', @activity_point, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger" %>


    </div>
  </div>
</div>