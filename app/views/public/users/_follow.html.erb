<%= form_with model: relationship, class: "like-form", local: false do |f| %>
    <!--コントローラーで保持しているidを次のアクションに裏側で渡す-->
    <%= hidden_field_tag :following_id, user.id %>
  <%= f.submit "依頼する", class: "button" %>
<% end %>



